# Gradle Properties
org.gradle.jvmargs = -Xmx1G
org.gradle.parallel = true

# Mod Properties
version = 1.0
maven_group = mod.torchbowmod
archives_base_name = torchbowmod

# Dependencies are managed at gradle/libs.versions.toml
